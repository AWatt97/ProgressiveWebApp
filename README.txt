Website can be hosted via the command "npx serve" on the command line in the root folder. A localhost link will then be copied to the clipboard

**SEE SCREENSHOT INSIDE ROOT FOLDER "PWA Lighthouse Assessment"** This is a test result run by the Google Lighthouse which determines if an app is a PWA and scores the app
accordingly. As seen in the image my app is considered a PWA, there was no other AMP or debugging options available other than this.